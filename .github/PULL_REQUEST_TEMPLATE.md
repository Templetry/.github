## What this changes

<!-- The what is in the diff. Use this space for the why: what was wrong, or
     what became possible. -->

## Verification

<!-- How you know it works. For a form or a piece, the CI job that built it. -->

- [ ] CI is green
- [ ] For a template change: the rendered output still builds, in every feature combination
- [ ] For an engine change: `gofmt`, `go vet` and `go test ./...` pass locally

## Anything worth recording?

<!-- A change to the model — a new kind, a manifest field, a different update
     strategy — wants an ADR alongside it. A bug fix does not. Delete this
     section if it does not apply. -->
